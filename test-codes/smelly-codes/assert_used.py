assert self.url, "All clients must have a URL attribute"

assert x, "x"
assert 16, '16'

assert x == "goodbye", "x should be goodbye"
assert y == getHello(), 'value should be x'
assert get() == 'x', 'value should be x'
assert isinstance(x, int), 'x should be int'