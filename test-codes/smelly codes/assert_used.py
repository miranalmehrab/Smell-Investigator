assert self.url, "All clients must have a URL attribute"
assert x, "x"
assert 16, '16'
assert x == y
assert sekf.url == 'asshd'
assert x == "goodbye", "x should be 'hello'"
assert y == getHello(), 'oshkjshd'
assert getHello() == x
assert get() == 'sadsd', 'asdj'
assert isinstance(x, int), 'x should be int'
